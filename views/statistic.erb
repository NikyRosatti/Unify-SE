<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Estadísticas del Documento</title>
  <link rel="stylesheet" href="/statistic.css">
</head>
<body>
  <div class="container">
    <h1>Estadísticas del Documento: <%= @document.filename %></h1>

    <div class="questions-container">
      <% @questions.each do |question| %>
        <div class="question-card">
          <h2><%= question.content %></h2>
          <p><strong>Total Responses:</strong> <%= question.number_answers_answered %></p>
          <p><strong>Total Correct Responses:</strong> <%= question.correct_answers_cant %></p>
          <p><strong>Correct Response Rate:</strong>
            <% if question.number_answers_answered > 0 %>
              <%= ((question.correct_answers_cant.to_f / question.number_answers_answered) * 100).round(2) %>%
            <% else %>
              0%
            <% end %>
          </p>
        </div>
      <% end %>
    </div>

    <a href="/view_docs" class="btn">Volver</a>
  </div>
</body>
</html>
