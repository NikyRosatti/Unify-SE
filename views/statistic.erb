<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Estadísticas del Documento</title>
  <link rel="stylesheet" href="statistic.css">
</head>
<body>
  <div class="container">
    <h1>Estadísticas del Documento: <%= @document.filename %></h1>

    <table>
      <thead>
        <tr>
          <th>Pregunta</th>
          <th>Total Respuestas Dadas</th>
          <th>Total Respuestas Correctas</th>
          <th>Porcentaje de Correctas</th>
        </tr>
      </thead>
      <tbody>
        <% @questions.each do |question| %>
          <tr>
            <td><%= question.content %></td>
            <td><%= question.number_answers_answered %></td>
            <td><%= question.correct_answers_cant %></td>
            <td>
              <% if question.number_answers_answered > 0 %>
                <%= ((question.correct_answers_cant.to_f / question.number_answers_answered) * 100).round(2) %>%
              <% else %>
                0%
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <a href="/" class="btn">Volver</a>
  </div>
</body>
</html>
